import { ConnectionConfiguration } from './connection-configuration';

describe('ConnectionConfiguration', () => {
  it('should create an instance', () => {
    expect(new ConnectionConfiguration()).toBeTruthy();
  });
});
